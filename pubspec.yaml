name: arctic_vpn
description: Arctic VPN - fake to real VPN
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.5.1          # ← добавь эту строку (или ^3.0.0-dev.x если используешь Riverpod 3)

  # твой git-пакет остаётся
  flutter_inset_shadow:
    git:
      url: https://github.com/chitochi/flutter_inset_shadow.git
      ref: main
  # flutter_launcher_icons: ... (твоя секция дальше)

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    # твои ассеты здесь, если есть