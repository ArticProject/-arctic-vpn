name: arctic_vpn
description: Arctic VPN - fake to real VPN
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Заменяем оригинальный пакет на рабочий форк (chitochi)
  flutter_inset_shadow:  # ← обрати внимание: flutter_inset_shadow (без box)
    git:
      url: https://github.com/chitochi/flutter_inset_shadow.git
      ref: main  # или укажи конкретный коммит, если main сломан

  # flutter_launcher_icons: ... (твоя секция дальше)

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    # твои ассеты здесь, если есть